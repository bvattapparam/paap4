import { PaapcurrencyPipe } from './paapcurrency.pipe';

describe('PaapcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PaapcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
